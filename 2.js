// Complex types for Javascript
// Arrays, functions, Object

//Arrays

var colors = ['Red', 'Blue', 'Green'];

var nos = [1, 2, 3];

var mixedArrays = [1, "name", true, null];
console.log(mixedArrays);
console.log(mixedArrays[1]);

var arrOfObjects = [colors, nos, mixedArrays];
console.log(arrOfObjects);

//Remember arrays are special type of objects in JS
//It has its own properties and methods. Will look into it later. 

